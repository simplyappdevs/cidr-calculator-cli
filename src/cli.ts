#!/usr/bin/env node
/**
 * CLI entrypoint
 */

/**
 * App Imports
 */
// import {Command, CommandImpl} from './command';
// import {default as execCLI} from './index';

// // exec CLI
// (function () {
//   // const cmd: Command = new CommandImpl('cidr', 'Parse CIDR');

//   // cmd.addArgument(['-c', '--cidr'], 'CIDR notation', 'N.N.N.N.N/CB', /.*/gi);
//   // cmd.addArgument(['-i', '--ipv4'], 'IPv4 address', 'N.N.N.N', /.*/gi)
//   //   .addArgument(['-cb', '--cidr-block'], 'CIDR block', 'CB', /.*/gi);

//   // console.log(JSON.stringify(cmd));
//   //return execCLI(process.argv.slice(2));
//   console.log('hello');
// })();

console.log('test');